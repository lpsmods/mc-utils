import { EntityQueryOptions } from "@minecraft/server";
import { EntityHandler } from "./EntityHandler";

// TODO:
export class BossHandler extends EntityHandler {
  
  constructor(options: EntityQueryOptions) {
    super(options);
  }
}
